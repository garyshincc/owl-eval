# Local Testing Environment - Safe for E2E testing
# Copy this to .env.local when you want to test locally

# Local URLs (no production impact)
NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Stack Auth - Create a separate test project
# Go to https://app.stack-auth.com and create a new project for testing
NEXT_PUBLIC_STACK_PROJECT_ID=test-project-id-here
NEXT_PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=test-client-key-here
STACK_SECRET_SERVER_KEY=test-server-key-here

# Local Database - Use a separate test database
# Option A: Local PostgreSQL
DATABASE_URL="postgresql://localhost:5432/evaluation_test"

# Option B: Separate Neon test database
# DATABASE_URL="postgresql://user:pass@test-db.neon.tech/testdb?sslmode=require"

# Test Storage - Separate bucket/directory
TIGRIS_BUCKET_NAME=test-bucket-or-local-storage
DATA_DIR=./test-data
VIDEO_UPLOAD_DIR=./test-uploads/videos

# Optional: Separate test API keys (or remove for local testing)
# PROLIFIC_API_TOKEN=test-token-or-leave-empty

# Test-specific settings
NODE_ENV=development
DEBUG_MODE=true
LOG_LEVEL=debug